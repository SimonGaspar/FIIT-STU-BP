namespace Bachelor_app.Enumerate
{
    /// <summary>
    /// Types of group in ListView (MainForm).
    /// </summary>
    public enum EListViewGroup
    {
        BasicStack,
        LeftCameraStack,
        RightCameraStack,
        DrawnKeyPoint,
        DrawnMatches,
        DepthMap,
        Console
    }
}
