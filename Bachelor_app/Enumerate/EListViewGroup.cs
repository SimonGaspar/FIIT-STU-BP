namespace Bakalárska_práca.Enumerate
{
    /// <summary>
    /// Type of group in ListView (MainForm)
    /// </summary>
    public enum EListViewGroup
    {
        BasicStack,
        LeftCameraStack,
        RightCameraStack,
        DrawnKeyPoint,
        DrawnMatches,
        DepthMap,
        Console
    }
}
