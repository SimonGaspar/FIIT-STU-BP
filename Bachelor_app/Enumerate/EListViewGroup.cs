namespace Bakalárska_práca.Enumerate
{
    // Check Resources for string value
    public enum EListViewGroup
    {
        BasicStack,
        LeftCameraStack,
        RightCameraStack,
        DrawnKeyPoint,
        DrawnMatches
    }
}
