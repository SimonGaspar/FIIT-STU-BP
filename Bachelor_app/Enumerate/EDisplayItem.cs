namespace Bakalárska_práca.Enumerate
{
    // Check Resources for string value
    public enum EDisplayItem
    {
        ListViewer,
        DepthMap,
        LeftCamera,
        RightCamera,
        PointCloud,
        KeyPoints,
        KeyPointsMatches
    }
}
